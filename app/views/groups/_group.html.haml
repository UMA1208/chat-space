= link_to group_messages_path(group.id) do
  .sidebar__groups-list__group--name
    = group.name
  .sidebar__groups-list__group--latest-message
    - if group.messages.last
      = group.messages.last.body
    - else
      まだメッセージはありません。
